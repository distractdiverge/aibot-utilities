# AIBot Utilities

These expect tools like 'git' and 'gh' (githubs cli tool) to be already installed and configured.

You must copy this project, or clone it to the path `~/.aibot-utilities/` to have it function correctly.

You must then also link or copy the claude command(s) to the `~/.claude/commands/*.md` path.
